当前要实现的目标：
1.理解我的架构
2.将注释中的行为函数完成
3.写一个能用的显示函数，仅渲染用户视野内的画面
4.每个细胞表现为一个圆形，其优先级越大圆形越大。同一个环境格内的细胞要显示在同一个环境格内，且不能重叠。应将所有细胞等比缩放到一定大小使其能恰好塞满方格。
5.当视角缩放到太广的时候（例如视角内的格子数超过了1w），则优化每个环境格内仅显示优先级最高的细胞。
要求能做到基础的游玩。
